<div class="page-content">
  <div class="container-fluid">
    <!-- member -->
    <% if (partial === "member") { %> <%- include("./member", { users }) %> <% }
    else if (partial === "update-member") { %> <%- include("./update-member", {
    user }) %> <% } %>
  </div>
</div>
